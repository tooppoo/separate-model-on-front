{
  "name": "@example/domain-cart",
  "version": "0.0.1",
  "description": "domain model for user, which is segregated from view",
  "author": "philomagi <tooppoo56@gmail.com>",
  "homepage": "",
  "license": "MIT",
  "main": "dist/index.js",
  "files": [
    "lib/"
  ],
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "prebuild": "rm -r dist",
    "prepublish": "npm run test && npm run build"
  },
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.3",
    "typescript": "^4.0.5"
  }
}
