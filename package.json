{
  "name": "segregated-model-on-front",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve:vue": "lerna run serve --scope=@example/vue --stream",
    "build:vue": "lerna run build --scope=@example/vue --stream",
    "serve:jquery": "lerna run serve --scope=@example/jquery --stream",
    "build:domain:cart": "lerna run build --scope=@example/domain-cart --stream",
    "test": "lerna run test --stream"
  },
  "keywords": [],
  "author": "philomagi<tooppoo56@gmail.com>",
  "workspaces": [
    "packages/**/*"
  ],
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
